git:
  host: github.com
  project: ryclarke
  default-branch: develop
repos:
  sort: true
  reviewers:
    cisco-batch-tool:
      - ryclarke
    another-repo:
      - ryclarke
  skip-unwanted: true
  unwanted-labels: deprecated,poc
  aliases:
    example:
      - cisco-batch-tool
      - another-repo
